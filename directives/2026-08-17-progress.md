# Build progress — 2026-08-17 CMO directives

Owner instruction (live, 2026-08-17/21): build the directives without waiting for
approval, keep a recurring loop working until every definition of done is met,
and never hold an article for approval. This file is the running ledger.
Updated by the CMO build loop on every pass.

## P1 — Traffic instrumentation: BUILT 2026-08-21, awaiting real-traffic verification
- [x] `page_views` table created in production (migration: `scripts/migrate-page-views.mjs`; also in `initSchema`).
- [x] `POST /api/pageview` endpoint: server-persisted, bot-filtered, privacy-first (no raw IP/UA stored, daily-rotating visitor hash, no cookies, no popups).
- [x] `PageviewTracker` mounted in the root layout: covers `blog/[slug]`, homepage, and all four funnel pages.
- [x] `kpi-report.mjs` traffic section: pageviews, uniques, top pages, per-funnel views, view-to-lead rate. Smoke-tested end to end locally (rows persisted, bot filtered, report renders).
- [ ] VERIFY after deploy: production traffic rows accumulating; next Monday's snapshot shows non-null traffic.

## P2 — Telegram + fail-loud pipeline: CODE BUILT, delivery blocked on token
- [x] `publish-article.mjs` sends a Telegram message per published article (best-effort, never blocks a publish).
- [x] `scripts/alert-failure.mjs` (`npm run newsroom:alert-failure`) for pipeline failures.
- [x] `kpi-report.mjs --telegram` posts the weekly summary.
- [ ] BLOCKED (owner action, the one true external dependency): create the bot via BotFather and set `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` in Vercel env and the routine env. All senders currently no-op with a logged warning.
- [ ] OWNER CONFIG: the daily newsroom routine ran without `DATABASE_URL` five mornings straight (Aug 17–21 briefs); add `DATABASE_URL` to that routine's configuration at claude.ai/code/routines. Interim: the CMO build loop publishes any stranded staged drafts on each pass.

## P3 — Distribution + heroes: ACTIVE
- [x] Convention: `content/social/<date>.md`, two channels per story, written same day (2026-08-17, 2026-08-21 done).
- [x] Hero audit 2026-08-21: 79 live articles, 0 missing, 0 broken.
- [ ] VERIFY: 7 consecutive publish days with same-day copy and 7 consecutive clean hero audits (loop tracks daily).

## Policy changes shipped (owner instruction)
- `publish-article.mjs` publishes `status='live'` (reverted the 2026-08-21 draft-staging regression; kept its new image fingerprint + durable-host checks).
- `workflows/newsroom-images.ts` now serves LIVE articles with missing/placeholder heroes (was: drafts awaiting human review).
- `.claude/skills/cren-copywriting` publication section and `directives/README.md` updated: no pre-publish human approval, ever; deterministic gate only; fix post-publish.

## Loop log
- 2026-08-21: initial build pass (this commit). Next passes: verify production pageviews, publish stranded drafts if any, daily social copy, hero audit, re-check Telegram env.
