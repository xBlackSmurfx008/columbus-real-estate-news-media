# Newsroom brief — 2026-07-07

## Run status: BLOCKED — no articles published

The daily pipeline could not reach the NeonDB database from this cloud
execution environment. Every access path failed:

1. **Neon serverless HTTP driver** (used by `scripts/recent-articles.mjs` and
   `scripts/publish-article.mjs`): the environment's network egress proxy
   returned `HTTP 403 — Host not in allowlist` for both
   `api.c-5.us-east-1.aws.neon.tech` and
   `ep-shy-truth-amtfxuif-pooler.c-5.us-east-1.aws.neon.tech`.
2. **Direct Postgres (psql, port 5432)** to the pooler host: connection
   silently dropped (timed out after 30s — TCP egress blocked).
3. **Site admin API** (`/api/admin/articles`): requires an admin login;
   no credentials are available in this environment.

Because step 3 of the pipeline (dedup check via `recent-articles.mjs`) and
step 8 (publish) both require the database, no research was published this
run. No duplicate-risk-unchecked content was drafted or inserted.

## Fix

In the Claude Code environment settings (network egress policy), allowlist:

- `api.c-5.us-east-1.aws.neon.tech`
- `ep-shy-truth-amtfxuif-pooler.c-5.us-east-1.aws.neon.tech`

(or switch the environment to a policy that permits these hosts), then
re-fire the routine.

## Sources checked

None fetched — the run stopped before the research step, since selected
stories could not be deduplicated against recent coverage or published.
