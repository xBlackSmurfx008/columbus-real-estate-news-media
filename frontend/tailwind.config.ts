import type { Config } from "tailwindcss";

/** Tailwind v4: design tokens live in app/globals.css (@theme). This file remains for tooling compatibility. */
const config = {} satisfies Config;

export default config;
