# Progress — 2026-09-14 directives

Owner instruction (live in-session, 2026-09-14): "ship them." This file records
what shipped the same day, what was already built and only needed verifying,
and exactly what remains blocked on credentials only the owner can provide.

## P1 — Deploy path + conversion layer

**Status: blocked on one owner action. Everything else is done and waiting.**

- Verified against production (`verify:site -- --target production`, run
  2026-09-14): the site is healthier than the Sep 13 record (13 passed, 3
  blocking, down from 6 — all 104 heroes now pass), but the 4 funnel pages
  (`/sell/your-home`, `/sell/investment-property`, `/invest/deploy-capital`,
  `/rent/find-a-home`) still miss the commercial disclosure, and
  `funnel_events` holds zero real events ever. Production runs a build that
  predates the conversion layer.
- Verified the conversion layer is complete on `main`: the
  `FunnelDisclosure` component is wired into the funnel pages, the root
  layout mounts `PageviewTracker` and `FunnelTracker` (verify:site
  `analytics-mounted` PASS), lead status transitions write `first_response_at`
  and funnel events, and `vercel.json` defines the lead-SLA cron. All of it
  activates on the next production deploy — no further code is needed.
- This session has **no deploy path**: `vercel` CLI is logged out, and none of
  `VERCEL_TOKEN`, a deploy hook URL, or Vercel project IDs exist in the
  environment. GitHub Actions cannot substitute (account-level runner block,
  documented in CLAUDE.md).
- **The one owner action:** either connect the Vercel project
  (`frontend` under scope `stephen-s-projects-96d9c6b4`) to this GitHub repo
  for deploy-on-push (Vercel dashboard → Project → Settings → Git), or put
  `VERCEL_TOKEN` in the session/routine environment so an automated session
  can run `npx vercel --prod`. Either one ships everything above within
  minutes.

## P2 — AM routine credential + duplicate safety

**Status: duplicate safety SHIPPED. Credential remains an owner action.**

- Shipped: `frontend/scripts/recent-articles.mjs` now falls back to the
  committed content snapshot (`content/snapshot/public-data.json`) when
  `DATABASE_URL` is absent, instead of exiting with an error. Tested both
  paths: with the DB and without it, the script prints the same dedupe list —
  104 all-time articles — and the fallback output carries a banner stating
  the snapshot date and that it is the MINIMUM covered set. The exact
  failure of Sep 13 (the blind AM session could not see the Aug 31 Green
  Oaks article and drafted a duplicate) is now impossible: that article
  appears in the credential-less list.
- Verified: `publish-article.mjs` already re-exports the snapshot after
  every successful publish, and the market-refresh scripts do the same, so
  the committed fallback stays at most one uncredentialed gap behind the
  live database.
- **Owner action still required:** add `DATABASE_URL` to the 06:33 routine's
  cloud environment (claude.ai/code → routine environment settings). The
  fallback makes blind runs safe; it does not make them able to publish.
  The Diocese Gay St. advance story (publish-by Sep 18–20) still needs a
  credentialed session to go live.

## P3 — Lead response loop

**Status: everything buildable SHIPPED. The send is one click in Gmail.**

- Shipped: the admin dashboard (`/admin`) now shows an unmissable red alert
  banner whenever any real (non-test) lead sits in `new` — count, age of the
  oldest, and a direct link to the lead queue — plus a permanent "New Leads
  (unanswered)" stat card. Lint and typecheck clean.
- Shipped: a complete, honest reply to the Sep 5 renter lead (Amina Osman,
  Dublin, 1BR under $1,000) is drafted in the owner's Gmail, ready to send:
  it owns the 9-day delay, gives the real Dublin rent picture ($1,860
  typical, from our own market data), points to income-restricted options
  including the Reserve at Maryland coverage, and asks two qualifying
  questions. The lead's `notes` field records the draft; its status remains
  `new` — honestly — until the email is actually sent.
- Verified already built on `main` (no work needed): the admin lead queue
  supports `new → contacted → qualified → won/lost` transitions, the first
  move off `new` stamps `first_response_at` (which populates the KPI
  report's Contacted / Avg first response columns), each transition writes
  a funnel event, lead capture calls `sendTelegramInquiry`, and a lead-SLA
  cron sweep is scheduled in `vercel.json`.
- **Owner actions:** (1) send the Gmail draft, then set lead #24 to
  `contacted` in /admin/leads — that single click writes
  `first_response_at`; (2) set `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID`
  (Vercel env + routine env) to activate the already-written notification
  path. The dashboard banner and SLA cron reach production with the next
  deploy (see P1).

## Addendum (same day, owner instruction): Resend + CRM email

Owner instruction (live in-session, 2026-09-14): use the Resend API with the
CRM so communication is easy. Shipped:

- `lib/email.ts` — one Resend transport for all outbound site email. Same
  contract as the Telegram path: best-effort, typed failures, missing key
  degrades to a logged no-op. Config: `RESEND_API_KEY`, `RESEND_FROM_EMAIL`,
  `LEAD_NOTIFY_EMAIL`, `EMAIL_REPLY_TO` (defaults to the site's
  `editor@columbusrealestatenews.com` identity).
- Owner notifications now go out by email AND Telegram, with identical text,
  on every lead, contact message, membership signup, and subscriber —
  whichever channel is configured delivers.
- **Reply from the queue**: `/admin/leads` now has a "Reply by email" button
  on every lead. It sends through Resend from the site's address (Reply-To
  routes answers back to a monitored inbox), and only after Resend accepts
  the message does it flip `new → contacted`, stamp `first_response_at`, log
  the send in the lead's notes, and write the funnel event. A failed send
  changes nothing — the KPI can never claim a response that didn't happen.
- The CRM's `emailGateway` (`src/agent/integrations/email.ts`, backing the
  `/crm` dashboard's sequences and outreach) now sends through Resend
  whenever `RESEND_API_KEY` is set (or `EMAIL_MODE=resend`); Gmail-API and
  staged modes remain for tests. Outreach sent through the CRM should be
  logged in `crm/outreach-log.md` per that folder's append-only convention.
- `scripts/test-email.mjs` — one-command smoke test:
  `RESEND_API_KEY=... node scripts/test-email.mjs --to you@example.com`.

**Owner setup for Resend (one-time, ~5 minutes):**
1. resend.com → verify domain `columbusrealestatenews.com` (add the DNS
   records Resend shows — SPF + DKIM).
2. Create an API key, set `RESEND_API_KEY` in the Vercel project env (and the
   routine env if automated sessions should send).
3. Optional: `LEAD_NOTIFY_EMAIL` to receive notifications somewhere other
   than editor@; `EMAIL_REPLY_TO` if replies should land elsewhere.
4. Run the smoke test above, then reply to lead #24 from /admin/leads.

Note: this session still has no `VERCEL_TOKEN` and the Vercel CLI here is
logged out — the code reaches production on the next deploy, wherever that
runs.

## The honest bottom line

Every line of code all three directives need now exists on `main`. The
property's entire conversion layer is one deploy away, and the deploy is one
credential away. The three owner actions, in order of leverage:

1. Connect Vercel deploy-on-push (or provide `VERCEL_TOKEN`).
2. Add `DATABASE_URL` to the 06:33 routine environment.
3. Send the drafted reply to the Sep 5 lead, flip its status, and set the
   two Telegram variables.
