{
  "schema": "cren-directive-event-v1",
  "item": "ci-secret-isolation",
  "at": "2026-09-26",
  "actor": "cmo-rebuild",
  "type": "update",
  "status": "in_build",
  "verify": "node --test frontend/tests/workflow-secrets.test.mjs exits 0, and the owner confirms in GitHub Settings > Environments that DATABASE_URL and the Telegram secrets exist only in the reporting environment, whose deployment branches are limited to main, with no repository-level copies left.",
  "note": "Code half built on branch claude/gracious-carson-ilbz9x: pull requests run a secret-free pr-checks job, and every job that uses a secret declares environment: reporting. A pull_request run executes the branch's own workflow file, so the settings half (moving the secrets into the main-only environment) is what makes the isolation hold. Until then, repository-level secrets stay reachable."
}
