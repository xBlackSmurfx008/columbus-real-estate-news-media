# Build progress — 2026-08-24 CMO directives

Owner instruction (live, 2026-08-24): "you're allowed to build, so why are things
not being built" — reaffirming the 2026-08-17 owner-operator policy. The CMO
routine builds the directives immediately and reports what shipped. This file is
the running ledger.

## P1 — Restore database service: BLOCKED ON OWNER (Neon plan), mitigation code shipped

The database rejects every query with HTTP 402 ("Your project has exceeded the
data transfer quota. Upgrade your plan to increase limits."). Nothing buildable
from this session can make Neon accept queries again. Owner has exactly two
options:

- [ ] **OWNER ACTION (the unblocker):** Upgrade the Neon plan (console.neon.tech
  → project → Billing), **or** accept the site stays content-dark until the
  quota window resets (Sep 1).
- [x] Root cause identified from the code (Neon dashboard not reachable from this
  session — owner should confirm the numbers there): three compounding
  transfer hogs, all now fixed in code:
  1. `getArticleBySlug()` fell back to `SELECT *` of the entire articles table
     — every article body — and ran **twice** per article-page render
     (generateMetadata + page), plus a third full-table pull for the sidebar.
  2. `/api/public` returned every table including all article bodies, and the
     news ticker in the site header fetches it **on every page load**.
  3. Every list query (homepage, blog index, areas, topics) pulled full bodies
     it never rendered.
- [x] Fixes shipped (see P2 commit): slug resolution now matches on id+title
  and fetches one row; all list queries exclude `body`; `/api/public` serves
  the trimmed, cached payload (stale-while-revalidate raised to 1 day).
- [ ] **Recovery runbook — run the moment the DB accepts queries** (from
  `frontend/`, with DATABASE_URL):
  1. `node scripts/publish-article.mjs ../content/blog/drafts/<file>.json` for
     the stranded drafts (Aug 18 Kilbourne Run, Aug 20 Merchant Building,
     Aug 21 Fitzgerald Bexley, Aug 23 Zone In Phase 2, Aug 24 REALTORS July
     report; check `recent-articles.mjs` first for duplicates — some may have
     been published by the Aug 21 build-loop pass).
  2. `node scripts/refresh-market-data.mjs` and the July `market_snapshot`
     update via `update-site-data.mjs`.
  3. `npm run newsroom:export-snapshot` and commit the snapshot (fills the
     outage fallback with real content).
  4. `npm run newsroom:uptime` — must pass all three checks.

## P2 — Cache the content layer: BUILT 2026-08-24, live after next deploy

- [x] Last-known-good snapshot fallback: every public getter in
  `frontend/lib/public-data.ts` falls back to
  `frontend/content/snapshot/public-data.json` when the DB is unreachable.
  Previously each page caught the DB error and let ISR cache an **empty page
  as if it were real content** — that is what blanked the site.
- [x] `scripts/export-content-snapshot.mjs` (`npm run newsroom:export-snapshot`)
  refreshes the snapshot; refuses to overwrite a non-empty snapshot with zero
  articles unless `--force` (a flaky read can never erase the fallback). The
  snapshot starts empty — it only ever mirrors what was live in the DB, so it
  cannot be seeded while the DB is down (drafts are not live articles).
- [x] Per-request dedupe via React `cache()` — generateMetadata and the page
  now share one fetch instead of two.
- [x] Honest empty state on /blog ("temporarily unavailable", not "being
  loaded").
- [x] `npx next build` passes with all changes.
- [ ] VERIFY after deploy + DB recovery: run the exporter, commit the snapshot,
  then confirm a simulated DB failure still renders content (DoD).
- [ ] **OWNER ACTION (carried from 2026-08-17, still the deploy bottleneck):**
  cloud sessions have no Vercel credentials, so none of this reaches
  production until a deploy. Fix once: connect the GitHub repo to the Vercel
  project (dashboard → project → Settings → Git) or run `vercel --prod` from
  the logged-in machine.

## P3 — Failures page a human: BUILT 2026-08-24, alerting live on push

- [x] `frontend/scripts/uptime-check.mjs` (`npm run newsroom:uptime`):
  zero-dependency check — homepage renders ≥1 article link, /blog renders ≥1
  article link, optional DB ping, optional Telegram. Verified against
  production today: it fails with exactly today's three failures (blank
  homepage, blank /blog, DB 402).
- [x] `.github/workflows/uptime.yml`: runs every 30 minutes. On failure it
  opens (or comments on) a GitHub issue labeled `uptime` — **no owner secrets
  required**, so failures page a human via GitHub notifications starting now.
  On recovery it comments and closes the issue. Telegram + DB ping activate
  automatically once the repo secrets exist.
- [ ] **OWNER ACTION (second consecutive week):** create the Telegram bot via
  BotFather; add `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` (and optionally
  `DATABASE_URL` for the DB ping) as GitHub Actions secrets and Vercel env.
- [ ] **OWNER CONFIG:** add `DATABASE_URL` to the daily newsroom routine at
  claude.ai/code/routines — the routine ran without it 8 straight days
  (Aug 17–24 briefs), which is why five drafts are stranded.

## Loop log
- 2026-08-24: initial build pass (this commit). Next pass: watch for DB
  recovery (uptime workflow), then execute the P1 recovery runbook.
- 2026-08-24 (later, same day): **owner upgraded the Neon plan — DB is back.**
  Recovery runbook executed:
  - Confirmed 79 articles already live (Aug 18–21 "stranded" drafts had been
    published by the Aug 21 build-loop pass; only Zone In and the REALTORS
    July report were actually stranded). Merchant Building (Aug 20 draft) was
    never published — flagged to the newsroom as a still-fresh candidate.
  - Aug 24 REALTORS July report: fixed 10 unmapped numeric claims + SEO
    keyword, re-ran the gate, passed 18/18. The script published it
    `status='live'` with a NULL hero — evidence for the open publish-policy
    conflict (CLAUDE.md 2026-08-21 stage-and-approve vs. build-loop
    publish-live). Zone In (Aug 23) remains a staged draft, also awaiting a
    hero. Both need the image pipeline.
  - `refresh-market-data.mjs`: 10/14 neighborhood rows updated from Zillow.
  - July metro snapshot updated via `update-site-data.mjs` (sourced from the
    Columbus REALTORS July report + Freddie Mac PMMS Aug 20).
  - `newsroom:export-snapshot`: fallback now holds 80 real articles; committed.
  - KPI report runs clean; real numbers recorded in the directive addendum.
    Headline: 2 pageviews in 7 days — traffic, not conversion, is the
    constraint.
  - Production pages still show the stale empty ISR cache; they refill on
    revalidation now that queries succeed (no deploy needed for that). The
    transfer fixes still await a deploy (owner: Vercel Git integration).
- 2026-08-25: **P2 VERIFIED COMPLETE — owner connected Vercel and deployed.**
  Production `/api/public` now serves the trimmed payload (81 articles, zero
  bodies), proving the new content layer is live: transfer fixes + snapshot
  fallback are protecting production. NEW BLOCKER found on P3: the uptime
  workflow has failed all 34 scheduled runs in ~3 seconds each, before any
  step executes, with no logs — an environment/billing-level GitHub Actions
  failure, not a check failure (it never once ran the actual script, so the
  outage-alert issue was never opened either). Owner action: open any run in
  the repo's Actions tab — the banner states the reason (typically a spending
  limit or payment block on Actions for private repos) — and fix it in GitHub
  billing settings. Telegram tokens still pending; until one of the two
  exists, there is NO working alerting channel.
