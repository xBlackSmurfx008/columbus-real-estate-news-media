# Newsroom brief Aug 25, 2026 (session 2)

Session started; about to install deps.

## CRITICAL — recurring policy/code conflict, still unresolved: not publishing live this session

`DATABASE_URL` was available this session (unlike session 1 today). Before running
`publish-article.mjs`, I checked whether the underlying blocker a prior session hit
(commit `03fa10b`, Aug 24 16:19 UTC, "halt before publish - policy/code conflict found")
had been fixed. **It has not.**

- `frontend/scripts/publish-article.mjs:178` still inserts new articles with
  `status: 'live'` unconditionally — there is no draft/staging path in the code. Last
  touched Aug 21 (`b2cda6a`); untouched since.
- This directly contradicts the current, checked-in `CLAUDE.md` publication policy
  (owner update, 2026-08-21): *"automation stages only... Never bypass the script,
  write a row directly, or change the staged article to `live` from the routine."*
- The Aug 24 commits `3a93f27` ("feat(newsroom): auto-publish completed articles",
  author DigiWealth) and `bb2d0cc` built a *separate*, more careful auto-publication
  gate (`frontend/lib/auto-publication.ts`) that explicitly requires the source
  article to already be `status='draft'`, a passing machine quality gate, a durable
  fingerprinted hero image, etc. But `publish-article.mjs` — the script this routine
  is told to call — was never rewired to insert as `'draft'`, so that gate is
  unreachable from the daily routine's own critical path. Running `publish-article.mjs`
  today would still publish straight to the live public site with zero human review
  and no image, the exact thing CLAUDE.md's current policy forbids.
- Per my own operating rules I do not take hard-to-reverse, publicly-visible actions
  (publishing unreviewed content to a live news site) against an explicit, current,
  checked-in policy, and this routine's instructions do not authorize me to modify
  `frontend/scripts/publish-article.mjs` myself. **So this session did not call
  `publish-article.mjs` and nothing was published live.**
- **Needs owner decision** (same ask as Aug 24, still open): either (a) fix
  `publish-article.mjs` to insert `status: 'draft'` and remove the auto-placeholder-hero
  block, restoring what `01ca461` did on Aug 21 before it was reverted, so the routine's
  drafts flow into the new auto-publication gate as designed; or (b) confirm explicitly
  that live-by-default is now the intended policy and update `CLAUDE.md` to say so,
  removing the contradiction.

## What this session did instead

- Ran `recent-articles.mjs` (DB reachable this session): 81 all-time published articles
  reviewed for duplicate-avoidance.
- Confirmed the Mercy on Main / Olde Towne East story flagged as "cover later" in
  today's session-1 brief is **already published** (Aug 2, 2026, id
  `2026-08-02-columbus-s-olde-towne-east-to-get-65-affordable-apartments-from-ex-nfl-player-s-`).
  Session 1 could not see this because `DATABASE_URL` was absent then. No action needed
  — just noting so a future session doesn't re-flag it.
- Web-searched for a fresh, verifiable real-estate story from the last 24-48h
  (Columbus Underground, Business First-adjacent coverage, columbus.gov, Franklin
  County Auditor). Found nothing new and clean enough to clear the duplicate/strength
  bar: the one apartment-conversion lead (Brewery District, 1036 S. Front St., Arcadia
  Development) turned out to date to September 2025, not new. Franklin County property
  reassessment coverage would duplicate the July 30 "how to challenge your value"
  article already published. Per CLAUDE.md: publish zero rather than force a weak or
  duplicative item. **Real estate slot: no story today.**
- Lifestyle slot: Session 1's Sugar restaurant draft
  (`content/blog/drafts/2026-08-25-columbus-park-street-sugar-restaurant-opens-august-27.json`)
  is well-sourced (5 sources, fact-checked, primary sugarcolumbus.com + 4 secondary) and
  remains the pick — reconfirmed, not re-drafted. It has **not** been staged/published
  for the reason above.
- Ran `refresh-market-data.mjs` (safe, additive DB write per CLAUDE.md — pulls free
  Zillow ZHVI/ZORI + FRED, never fabricates, only writes what it resolves):
  10/10 mapped neighborhoods updated (Westerville, Hilliard, Dublin, Upper Arlington,
  Gahanna, New Albany, Worthington, Bexley, German Village, city avg). ZORI
  neighborhood-level rent file 404'd (Zillow-side, not fixable here) — city-level rent
  ($1,450) still updated. FRED 30-yr mortgage rate: no update this run — the CSV parse
  found no valid data row (network/proxy access to fred.stlouisfed.org may be
  restricted in this environment); left unchanged rather than guessing.
- Skipped the ticker refresh (`update-site-data.mjs`) — today's research did not turn
  up enough freshly-verified headline-worthy items to populate 5-6 ticker entries
  without padding, and CLAUDE.md says never fabricate.
- Skipped image generation (Higgsfield) — no article was staged this session, so there
  is nothing to attach a hero to yet.
- Skipped email (Gmail) — no article published. Owner notified via the routine's push
  notification instead, per "owner prefers Telegram... for notifications about drafts,
  errors, and reports" (Telegram not available as a tool in this session; push used as
  the closest available channel).

## Net result

0 articles published this session (consistent with session 1). 1 fully-sourced draft
on disk, unstaged, pending the publish-article.mjs fix or an explicit owner policy
call. Market data refreshed. No fabricated content.

## UPDATE (owner live, ~16:20 UTC): conflict resolved this session

The owner responded live and directed a fix. Changes made:

- `frontend/scripts/publish-article.mjs` now inserts `status='draft'` (was `'live'`),
  restoring the staging behavior of `01ca461` per the checked-in CLAUDE.md policy.
  The auto-placeholder-hero block and "Published live" messaging were removed; the
  Telegram alert now reports "Staged draft" and links to the admin review page.
- `frontend/tests/image-pipeline.test.mjs` regression guard updated: it now asserts
  publish-article.mjs attaches no hero at all and inserts `'draft'` (stronger than the
  old assertion, whose premise was the removed placeholder block). All 31 .mjs tests pass.
- The Sugar draft was then staged through the fixed script. The deterministic gate
  first blocked it on three real findings (missing `central-ohio-real-estate` tag, an
  unledgered Zone In date sentence, keyword not in opening). Fixed all three in the
  draft JSON — the Zone In dates were re-verified against the columbus.gov news release
  (HTTP 200, fetched this session) and added as S6/C11. Gate now passes 18/18.
- **Staged:** `2026-08-25-sugar-opens-aug-27-at-504-park-st-in-columbus-arena-district`,
  confirmed `status='draft'` in the DB, `image_url` null. It goes live via the
  auto-publication gate once the image workflow attaches a durable fingerprinted hero,
  or via explicit approval at
  https://columbusrealestatenews.com/admin/articles?edit=2026-08-25-sugar-opens-aug-27-at-504-park-st-in-columbus-arena-district
